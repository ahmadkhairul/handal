import React from "react";

const Icon = ({ src }) => {
  return (
    <img
      style={{ marginLeft: "5px" }}
      src={src}
      alt="handal-icons"
      width="10px"
      height="10px"
    />
  );
};

export default Icon;
